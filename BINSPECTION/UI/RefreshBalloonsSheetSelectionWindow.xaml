<Window x:Class="BINSPECTION.UI.RefreshBalloonsSheetSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="RootWindow"
        Title="Refresh Balloons"
        Height="420" Width="380"
        MinHeight="280" MinWidth="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"
                   Text="Check which sheets to refresh. Every balloon on a checked sheet is removed and redrawn against current geometry; sheets left unchecked are not touched. Saved balloon data is not changed." />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
            <Button Content="Select All" Width="80" Margin="0,0,4,0" Click="SelectAll_Click" />
            <Button Content="Select None" Width="80" Click="SelectNone_Click" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  x:Name="SheetsGrid"
                  ItemsSource="{Binding Sheets, ElementName=RootWindow}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="False"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Refresh" Binding="{Binding IsChecked}" Width="70" />
                <DataGridTextColumn Header="Sheet" Binding="{Binding Name}" IsReadOnly="True" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Refresh" Width="80" Margin="0,0,8,0" IsDefault="True" Click="Refresh_Click" />
            <Button Content="Cancel" Width="80" IsCancel="True" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
